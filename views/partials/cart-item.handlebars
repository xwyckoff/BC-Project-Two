<div class="ui card">
    <div class="content">
        <h1 style="color:black;">{{product.product.product_name}}</h1>
        <h2 class="ui sub header">{{product.product.brand.brand_name}}</h2>
    </div>
    <div class="content">
        <div class="content">
            <div style="color:black;">
                {{product.product.product_description}}
            </div>
            <h5 class="ui sub header">{{product.product.product_price}}</h5>
        </div>
    </div>
    <div class="ui bottom attached button">
        <i class="trash icon"></i>
        Remove from Cart
    </div>
</div>